function teste1(num) {
    // caso não use {} para definir um bloco, o if considera apenas uma 
    // sentença de código abaixo dele
    // a segunda será sempre executada, independente do resultado
    if(um > 7)
        console.log(num)

    console.log('Final')
}
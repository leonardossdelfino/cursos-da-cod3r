const somar = (a, b) => {
    return a + b
}

console.log(somar(2, 3))

const escreva = (texto) => {
    console.log(texto)
}

escreva('Leo')

const media = (nota1, nota2, nota3, nota4) => {
    return (nota1 + nota2 + nota3 + nota4) / 4
}

console.log('A média é: ' + media(2, 3, 5, 6))